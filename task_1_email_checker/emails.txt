# Валидный адрес (Google)
support@google.com

# Адрес с гарантированно несуществующим доменом (для проверки ошибки DNS)
user@non-existent-domain-xyz-123.com

# Реальный домен, но с опечаткой в синтаксисе (для проверки Regex)
bad-email.at.domain.ru

# Домен без MX-записей (например, технический поддомен)
admin@localhost
